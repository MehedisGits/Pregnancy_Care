<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ZenMomPregnancy" parent="Theme.Material3.Light.NoActionBar">

        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/splash</item>
        <!-- Customize other splash screen styles if needed -->
    </style>

    <style name="Theme.ZenMomPregnancy" parent="Base.Theme.ZenMomPregnancy" />
</resources>